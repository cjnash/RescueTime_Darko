<%= form_for(@darko) do |f| %>
  <% if @darko.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@darko.errors.count, "error") %> prohibited this darko from being saved:</h2>

      <ul>
      <% @darko.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'What did Darko just ask you about?' %><br />
    <%= f.text_area :status, "rows" => 3, "cols" => 60 %>
  </div>
  <div class="actions">
    <%= f.submit 'Add your update' %>
  </div>
<% end %>